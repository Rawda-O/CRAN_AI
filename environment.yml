name: TorchGPU
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11.8
  - pip=25.0
  # --- PyTorch + CUDA (pinned)
  - pytorch=2.5.0
  - torchvision=0.20.0
  - torchaudio=2.5.0
  - pytorch-cuda=12.4

  # --- Core scientific stack (pinned)
  - numpy=2.0.2
  - scipy=1.14.1
  - pandas=2.2.3
  - pyyaml=6.0.2
  - scikit-learn=1.5.2

  # --- Visualization (pinned)
  - matplotlib=3.10.8
  - seaborn=0.13.2

  # --- Utilities (pinned)
  - tqdm=4.67.1
  - rich=13.9.4

  # --- Dev / testing / notebooks (pinned)
  - pytest=8.3.4
  - jupyter=1.1.1
  - ipykernel=6.29.5
